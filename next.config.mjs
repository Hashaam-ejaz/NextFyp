/** @type {import('next').NextConfig} */
const nextConfig = {
    
    images: {
        domains: ['file'], // Allow images from the "file://" protocol
        
    }
};
 


export default nextConfig;
