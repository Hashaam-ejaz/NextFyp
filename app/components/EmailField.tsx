import React from "react";

const EmailField = () => {
  return (
    <input
      className="mt-[0.688rem] mb-[0.938rem] bg-gray-100 h-[2.813rem] p-4 w-full"
      type="text"
      name="email"
      id="email"
      placeholder="Email or phone number"
    />
  );
};

export default EmailField;
